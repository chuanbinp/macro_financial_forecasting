name: agentic-macro-fincast
channels:
  - defaults
  - conda-forge
  - nvidia
dependencies:
  - python=3.11
  - pip
  - tqdm
  - pandas
  - numpy
  - pyarrow
  - pytorch
  - pip:
      - instructor
      - datasets
      - pydantic>=2.0
      - transformers
      - python-dotenv
      - feedparser
      - yfinance
      - joblib
      - langgraph
      - langchain-core
      - langchain-openai
      - streamlit
      - plotly
      - nest_asyncio
      - scikit-learn
