# from pydantic import BaseModel, Field

# class NewsSummary(BaseModel):
#     summary: str = Field(description="A concise summary of the news articles in 5 to 10 most important points for a finance audience.")